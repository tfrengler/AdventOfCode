{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build zig",
            "type": "shell",
            "command": "zig",
            "args": [
                "build-exe",
                "${file}",
                "-femit-bin=${workspaceFolder}\\bin\\${fileBasenameNoExtension}.exe"
            ]
        }
    ]
}