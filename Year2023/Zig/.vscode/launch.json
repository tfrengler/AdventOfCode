{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB - debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/${fileBasenameNoExtension}.exe",
            "args": [],
            "terminal": "integrated",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build zig"
        }
    ]
}