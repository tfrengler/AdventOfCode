{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB - debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\${fileBasenameNoExtension}.exe",
            "args": [],
            "terminal": "external",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Clang - build"
        }
    ]
}