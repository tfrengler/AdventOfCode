{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB - debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\${fileBasenameNoExtension}.exe",
            "args": [],
            "terminal": "integrated",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Clang - build"
        }
    ]
}