{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB - debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\bin.exe",
            "args": [],
            "terminal": "integrated",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Clang - build"
        }
    ]
}